{"question": "PLACEHOLDER — How do I install the project and start the development server?", "expected_paths": ["docs/setup.md"], "note": "PLACEHOLDER template row — replace every row in this file with real questions about YOUR ingested corpus. See tests/eval/README.md."}
{"question": "PLACEHOLDER — What environment variables configure the database connection?", "expected_paths": ["docs/configuration.md", "docs/deployment.md"], "note": "PLACEHOLDER — expected_paths may list several documents; a hit on ANY of them counts. Paths must match the chunk metadata document_path exactly (relative, forward slashes)."}
{"question": "PLACEHOLDER — What does the authenticate_user function return on invalid credentials?", "expected_paths": ["src/auth/service.py"], "note": "PLACEHOLDER — code questions work too; document_path for code files is the ingest-relative path."}
{"question": "PLACEHOLDER — Which HTTP status codes can the /api/orders endpoint return?", "expected_paths": ["docs/api_reference.md"], "note": "PLACEHOLDER — good golden questions have one clearly correct source document."}
{"question": "PLACEHOLDER — What is the retry policy for failed webhook deliveries?", "expected_paths": ["docs/webhooks.md"], "note": "PLACEHOLDER — prefer questions you actually asked in real sessions (mine them from the SQLite messages table)."}
{"question": "PLACEHOLDER — How are user passwords hashed and validated?", "expected_paths": ["src/auth/hashing.py", "docs/security.md"], "note": "PLACEHOLDER — multi-document answers: list every document that should surface."}
{"question": "PLACEHOLDER — What are the rate limits for the public API?", "expected_paths": ["docs/rate_limits.md"], "note": "PLACEHOLDER — keep questions specific; vague questions make hit-rate meaningless."}
{"question": "PLACEHOLDER — Summarize the architecture of the ingestion pipeline.", "expected_paths": ["docs/architecture.md"], "note": "PLACEHOLDER — broad questions are fine as long as the expected document is unambiguous."}
{"question": "PLACEHOLDER — What versions of Python does the project support?", "expected_paths": ["README.md", "pyproject.toml"], "note": "PLACEHOLDER — top-level files use their bare relative name as document_path."}
{"question": "PLACEHOLDER — How do I rotate the signing keys in production?", "expected_paths": ["docs/operations/key_rotation.md"], "note": "PLACEHOLDER — nested paths keep their full relative form. Delete or replace ALL placeholder rows before trusting any numbers."}
