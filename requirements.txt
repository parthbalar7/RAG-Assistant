# Core AI
anthropic
ollama
sentence-transformers
transformers>=4.48  # gte-modernbert support
chromadb
posthog<6  # chromadb 1.5.x requires posthog<6.0.0; 6+/7+ change capture() and spam telemetry TypeErrors
tiktoken
# Hybrid search
bm25s>=0.2
rank-bm25

# Knowledge graph
networkx
spacy>=3.7  # LLM-free NER graph extraction; run: python -m spacy download en_core_web_sm

# Learned query router
scikit-learn
joblib

# AST-aware code chunking (tree-sitter based; falls back to regex chunking if absent)
astchunk

# Multimodal - PDF & Images
PyMuPDF
Pillow

# API
fastapi
uvicorn
websockets
python-multipart

# Auth
bcrypt
PyJWT

# Utilities
pydantic
pydantic-settings
rich
tenacity
requests

# Web augmentation (gap-driven research)
ddgs

# PageIndex-style local engine (no external dependencies — uses Ollama + PyMuPDF)
